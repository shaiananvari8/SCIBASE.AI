# Acceptance Notes

Reviewer acceptance checklist:

- The risky candidate is blocked for venue-level integrity signals.
- The borderline candidate requires a warning before insertion.
- The reputable candidate is allowed.
- Audit digests are deterministic across repeated runs.
- Demo artifacts are generated locally from synthetic data only.

Validation commands:

```bash
npm run check
npm test
npm run demo
npm run demo:video
```
